{
  "name": "45-smart-quotes",
  "version": "1.0.0",
  "description": "- an endpoint `/quote` that sends to the client a random quote to rate.\r - an endpoint `/rate-quote` that receive and quote id and rating between 1 to 5.\r - an endpoint `/` that show all the quotes and their ratings.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
